import type { CubeState, Move } from '../domain'

/**
 * Solver layer stub — returns an empty move sequence until algorithms are implemented.
 */
export function solve(cube: CubeState): Move[] {
  void cube
  return []
}
